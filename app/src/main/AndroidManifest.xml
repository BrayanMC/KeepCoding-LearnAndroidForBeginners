<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bmc.baccus">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".controller.activities.WineActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".controller.activities.WebActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:parentActivityName=".controller.activities.WineryActivity"
            android:label="@string/app_name"
            tools:targetApi="jelly_bean" />

        <activity
            android:name=".controller.activities.SettingsActivity"
            android:label="@string/app_name" />

        <activity
            android:name=".controller.activities.WineryActivity"
            android:label="@string/title_activity_winery">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>